version: '3.8'

services:
  ollama:
    build:
      context: ./ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - ai-net

volumes:
  ollama_data:

networks:
  ai-net:
